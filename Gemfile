source "https://rubygems.org"

# GitHub Pages builds the site for you using this gem set. Keeping it here lets
# you optionally preview the site locally with the same versions GitHub uses.
gem "github-pages", group: :jekyll_plugins

# Plugins used by the site
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end
